# 更新日志

本文记录 astrbot_plugin_vrc_tool 的版本变更，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.0.6] - 2026-09-22

### 新增

- 指令 `/vrc原始 <昵称或玩家ID>`：输出 VRChat API 返回的玩家原始 JSON，便于排查字段
  （例如信誉相关的 `tags`）。仅管理员可用，且仅在私聊生效，群聊触发会被拒绝。

## [1.0.5] - 2026-09-22

### 变更

- 玩家信誉等级改为与 VRCX 的 `computeTrustLevel` 逻辑对齐：
  仍从 `User.tags` 推断（VRChat API 不返回 `trustLevel` 字段，VRCX 也是同样做法），
  `system_trust_veteran` / `system_trust_trusted` / `system_trust_known` / `system_trust_basic`
  依次对应 可信玩家 / 知名玩家 / 玩家 / 新玩家，无 trust 标签为 游客。
- `system_troll`、`system_probable_troll` 不再覆盖信誉等级，改为「劣迹」「疑似劣迹」附加标注。
- 新增开发者身份标注：`admin_moderator` 标签，或 `developerType` 为
  `moderator` / `internal` / `trusted` 时，分别标注 管理员 / 官方人员 / 可信开发者。
- 玩家信息中信誉行变为附带标注的格式，例如 `▸ 信誉　　：可信玩家（劣迹）`。

> 说明：1.0.4 未对外发布，版本号直接跳到 1.0.5。

## [1.0.3] - 2026-09-03

### 新增

- 指令 `/vrc昵称同步 [开|关]`：审核通过后把入群答案同步为该成员的群昵称（仅管理群可用）。
- 指令 `/vrc添加 玩家群:管理群1,管理群2`：运行时动态添加「玩家群 → 管理群」映射，无需改配置（仅管理员）。
- 玩家信息新增字段：在线状态、信誉等级、简介、账号创建日期、正在使用的模型、正在展示的群组。

### 变更

- 玩家 / 地图查询改为昵称精准匹配，避免匹配到相近昵称的对象。
- `/vrc登录`、`/vrc验证`、`/vrc添加` 等敏感指令限制为管理员权限。

## [1.0.2] - 2026-09-03

### 新增

- 插件标签：VRChat、娱乐、工具，便于在插件市场检索。

## [1.0.1] - 2026-09-02

### 变更

- 仅更新插件版本号，功能无变化。

## [1.0.0] - 2026-09-02

### 新增

- 首个版本：VRChat 登录（含邮箱 / TOTP 两步验证）、查询玩家、查询地图、玩家群入群审核。
- 插件图标 logo.png。
